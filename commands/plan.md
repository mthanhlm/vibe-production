---
description: Plan a feature before code — interview, explore, write a 1–3 page plan with verifiable "Done means" criteria, get approval, render the bilingual HTML view.
argument-hint: "[quick] [what to build]"
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/vibe-plan/SKILL.md` now and execute it
exactly as written. Where it says `$ARGUMENTS`, substitute: $ARGUMENTS
