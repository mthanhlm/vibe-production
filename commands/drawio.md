---
description: Turn a topology into a professional, editable draw.io diagram (opens in diagrams.net) — anti-slop design system (one idea, short labels, single accent, orthogonal edges), graphviz layout with a hand-grid fallback, and a mandatory render-and-review quality gate. No heavy install.
disable-model-invocation: true
---

Invoke the Skill tool now with skill `vibe:drawio`.

The tool loads and runs the skill natively — do not read or paraphrase the
skill file yourself.
